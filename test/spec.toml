# Comment

# String
string = "string\n\t\"string"
string_with_comment = "string with #comment"
string_with_comment_afterwards = "string" # ignore this comment

# Integer
integer = 42

# Float
pi = 3.14159

# DateTime
datetime = 1979-05-27T07:32:00Z

# Simple array
simple_array = [1, 2, 3]

# Nested array
nested_array = [[[1], 2], 3, [4]]

# String array
string_array = ["a", "b", "c"]

# Array with comment in string
array_with_comment = ["a", "b#b", "#c"]

# Array with closing bracket in string
array_with_closing_bracket = ["a", "b]b", "#c"]
array_with_closing_bracket_and_ending_comment = ["a", "b]b", "#c"] #comment

# Keygroup
[a.b.c]
d = "test"

[e]
f = "test"
